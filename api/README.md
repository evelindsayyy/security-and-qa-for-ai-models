# API (`api/`)

Flask REST + background job launcher (planned W5). Reuses the same `subprocess` /
Docker launch path as [`frontend/`](../frontend/). Until then: UI in `frontend/`; efficacy Postgres via
[`evaluator/db/`](../evaluator/db/README.md).
