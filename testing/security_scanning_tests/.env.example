#   cd testing/security_scanning_tests
#   cp .env.example .env
#   sed -i "s/^UID=.*/UID=$(id -u)/" .env
#   sed -i "s/^GID=.*/GID=$(id -g)/" .env
#
# docker compose reads this file automatically — stops container writing root-owned output

UID=1000
GID=1000
MODEL_ID=distilbert-base-uncased

# optional — faster hugging face downloads:
# HF_TOKEN=hf_...
