# Legacy benchmark path

Public benchmark runners moved to [`benchmarks/`](../../benchmarks/).

Old outputs may remain in `test_results/` here; the frontend reads them as a
fallback. Run new benchmarks via `/benchmarks/new` or `benchmarks/run_benchmark.py`.
