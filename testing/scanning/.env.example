# cd testing/scanning && cp .env.example .env
# sed -i "s/^UID=.*/UID=$(id -u)/" .env && sed -i "s/^GID=.*/GID=$(id -g)/" .env

UID=1000
GID=1000
MODEL_ID=distilbert-base-uncased

# optional — faster Hugging Face downloads:
# HF_TOKEN=hf_...
