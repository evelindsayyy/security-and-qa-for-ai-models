# Benchmark manifest (reference)

The canonical manifest for implemented runners lives at
[`benchmarks/manifest.yaml`](../../benchmarks/manifest.yaml).

This copy is kept for Track B planning docs; update the `benchmarks/` copy when
adding new benchmark pilots.
